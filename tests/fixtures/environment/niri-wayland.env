# Controlled Niri mixed-backend variant: the GNOME identity is explicit so the
# GNOME portal descriptor's legacy UseIn=gnome remains eligible.
XDG_CURRENT_DESKTOP=niri:GNOME
XDG_SESSION_DESKTOP=niri
XDG_SESSION_TYPE=wayland
WAYLAND_DISPLAY=wayland-1
