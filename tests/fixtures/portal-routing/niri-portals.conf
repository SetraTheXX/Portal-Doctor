[preferred]
# Mirrors the upstream Niri default: GNOME first, GTK fallback.
default=gnome;gtk;
org.freedesktop.impl.portal.Access=gtk;
org.freedesktop.impl.portal.Notification=gtk;
# Secret=gnome-keyring is intentionally outside this fixture: the current
# passive model has no Secret-service-specific runtime/unit contract.
