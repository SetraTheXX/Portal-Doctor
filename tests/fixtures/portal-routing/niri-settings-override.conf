[preferred]
# Higher-precedence user config used as the complete effective config for the
# XDP 1.22.0 regression fixture. Lower generic candidates are not merged.
default=gnome;gtk;
org.freedesktop.impl.portal.Access=gtk;
org.freedesktop.impl.portal.Notification=gtk;
org.freedesktop.impl.portal.Settings=gtk;
